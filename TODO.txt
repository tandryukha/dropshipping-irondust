- add conflicts/warnings to the injestions response (ConflictDetector to emit Warn entries for contradictions and missing criticals.)
- docker compose has some strange base images: FROM maven:3.9-eclipse-temurin-21 AS build FROM eclipse-temurin:21-jre
- should be able to index/reindex one product id/list
- test coverage to protect what we have now at least on the backend?
- domain structure of the packages
- make enrichment model configurable



- api first - start with spec file, generate interfaces, controllers should implement them
- documentation - use .md files only, keep it simple, remove docs server ✅
- create .http files instead of postman and keep them up-to-date ✅